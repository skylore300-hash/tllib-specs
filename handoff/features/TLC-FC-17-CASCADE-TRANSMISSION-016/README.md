# TLC-FC-17-CASCADE-TRANSMISSION-016 — Lineage edge-weight evaluator

This finalized Feature Handoff Package exposes the Domain 17 Cascade Transmission feature **Lineage edge-weight evaluator**. Its normative boundary is `maths/17-cascade-transmission/cascade-transmission-system.md` and the explicitly declared provider/ownership relations.

Evaluate w_L(e)=exp(-lambda generation(e))*phi(t_L(e)) only with supplied provider semantics.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Confirmed scientific provider domains: `[]`. Unresolved scientific relation domains: `[]`. Runtime domain dependencies are empty.

Forbidden: Do not invent generation indices, phi, time units or lambda calibration.
