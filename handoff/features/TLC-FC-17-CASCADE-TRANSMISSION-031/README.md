# TLC-FC-17-CASCADE-TRANSMISSION-031 — Cascade-local invariant-core N_inv(t) descriptor

This finalized Feature Handoff Package exposes the Domain 17 Cascade Transmission feature **Cascade-local invariant-core N_inv(t) descriptor**. Its normative boundary is `maths/17-cascade-transmission/cascade-transmission-system.md` and the explicitly declared provider/ownership relations.

Preserve the exact source set comprehension defining N_inv(t).

Scientific status: `partially_defined`. Execution status: `structural_only`. Confirmed scientific provider domains: `[]`. Unresolved scientific relation domains: `[4]`. Runtime domain dependencies are empty.

Forbidden: Do not replace it with domain-04 invariants or invent distance/value/tolerance providers.
