# TLC-FC-18-EVALUATION-005 — Integrated progression index

This handoff preserves the source integrated index across technical, contextual, ethical and reflexive components. The `r` component is mandatory even though the chapter defines no `m_r,j` family; a complete evaluation therefore requires an external reflexive metric provider. Normalizers, metric minima/maxima and interaction parameters are likewise supplied, never invented. Zero metric ranges are errors. The package forbids dropping `M_r`, inventing reflexive metrics, creating a normalizer, or silently clamping an undefined term.
