# TLC-FC-19-REGULATION-017 — Innovation bifurcation condition

Evaluate `B_+` only from supplied `lambda_max(J_collective)`, `dE_cohort/dt`, breakthrough/confidence thresholds and confidence value. Preserve all three strict `>` comparisons and the conjunction. Do not construct a Jacobian, solve an eigenvalue, numerically differentiate, or calibrate thresholds.
