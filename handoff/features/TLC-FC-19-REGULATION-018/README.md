# TLC-FC-19-REGULATION-018 — Systemic-crisis condition

Evaluate `B_-` from supplied `lambda_min(J_confidence)`, tension norm, transparency and thresholds. Preserve `lambda_min < 0 AND (||T_tensions|| > theta_crisis OR T_transparency < theta_transparency)` exactly, including boolean grouping and strict comparisons. Do not construct a Jacobian, solve an eigenvalue, infer a norm, or calibrate thresholds.
