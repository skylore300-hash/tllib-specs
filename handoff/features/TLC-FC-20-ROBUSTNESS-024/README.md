# TLC-FC-20-ROBUSTNESS-024 — Dual structural robustness ambiguity guard

This finalized guard preserves the source fact that `R_struct` has two incompatible definitions: a minimal perturbation-radius expression and a graph-spectral expression. Their formal relationship is not supplied. The package forbids equality claims, averaging, conversion, canonical selection, fallback selection, or any synthesized bridge between them.
