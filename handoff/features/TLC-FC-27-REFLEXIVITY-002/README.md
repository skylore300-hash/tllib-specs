# TLC-FC-27-REFLEXIVITY-002 — Source coherent-identity operator exact expression

This package exposes only the literal source operator `I(X,R,t)=(X,R+alpha(t)(R-X)+beta(t) grad_R Phi_id)`. Both added terms remain positive exactly as written, even though the source interprets them as recall toward reality and dissonance minimization. The package does not silently repair either sign and does not claim that this raw expression reduces `Phi_id`.
