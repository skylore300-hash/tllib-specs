# TLC-FC-27-REFLEXIVITY-009 — Reflexive semantic ambiguity and crisis-lifecycle guard

This package is the explicit non-execution guard for the source contradiction. It keeps `+alpha(R-X)` and `+beta grad Phi_id` visible alongside the later `-grad Phi_id` correction RHS and the positive social terms. A request for semantic dissonance reduction requires an explicit external mode/provider; a crisis lifecycle requires an external lifecycle provider. No formula wins silently and no state machine is invented.
