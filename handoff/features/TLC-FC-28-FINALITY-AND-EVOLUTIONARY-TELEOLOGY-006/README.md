# Invariant-core preservation condition and set-projection guard

Preserves the strict source condition and keeps `pi_N(G)` opaque. The package exposes the preservation check without inventing set-level projection semantics, centroids, Hausdorff distance, or any replacement geometry. All unresolved projection meaning remains an external scientific/provider boundary.
