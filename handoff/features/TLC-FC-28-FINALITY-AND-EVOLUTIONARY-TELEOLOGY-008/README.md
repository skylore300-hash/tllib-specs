# Preservation-penalty direction ambiguity guard

Preserves `+ Psi(P_pres)` exactly as written in the source. The direction of the preservation penalty remains scientifically unresolved, so this package does not invert the sign, alter the argument, impose monotonicity, or replace the term with an equivalent objective. The ambiguity is intentionally retained as part of the contract.
