# TLC-FC-30-EXPANSION-002 — Generational graph and replication ownership boundary

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Generational graph and replication ownership boundary**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Reuse domain-29 G_t and replication semantics as provider-owned inputs; do not redefine them in Expansion.

Scientific status: `defined`. Execution status: `structural_only`. Scientific provider domains: `[29]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
