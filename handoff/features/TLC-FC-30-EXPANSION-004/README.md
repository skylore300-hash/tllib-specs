# TLC-FC-30-EXPANSION-004 — Local-radiation mean evaluator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Local-radiation mean evaluator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Evaluate Phi only when the source-required community population and E_comm values are supplied and N is nonzero.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Scientific provider domains: `[]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
