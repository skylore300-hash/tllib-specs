# TLC-FC-30-EXPANSION-006 — Wave-front expansion evaluator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Wave-front expansion evaluator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Preserve v approximately 2 sqrt(D(gamma Phi-mu)) and gamma Phi > mu; do not invent validity bounds or solver semantics.

Scientific status: `partially_defined`. Execution status: `conditionally_executable`. Scientific provider domains: `[]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
