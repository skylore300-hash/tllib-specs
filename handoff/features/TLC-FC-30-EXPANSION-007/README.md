# TLC-FC-30-EXPANSION-007 — Translation invariant-preservation validator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Translation invariant-preservation validator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Validate pi_N(Msg prime)=pi_N(Msg) using domain-04 provider semantics.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Scientific provider domains: `[4]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
