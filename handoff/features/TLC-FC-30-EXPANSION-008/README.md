# TLC-FC-30-EXPANSION-008 — Translation-quality evaluator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Translation-quality evaluator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Preserve Q_trad exactly with supplied norm, lambda, and domain-24 d_C; do not normalize its result.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Scientific provider domains: `[24]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
