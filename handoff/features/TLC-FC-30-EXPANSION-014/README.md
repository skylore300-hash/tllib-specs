# TLC-FC-30-EXPANSION-014 — Resistance aggregate evaluator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Resistance aggregate evaluator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Compute source-local R_resistance as R_cult + R_soc + R_pol + R_eco from supplied components.

Scientific status: `defined`. Execution status: `executable`. Scientific provider domains: `[]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
