# TLC-FC-30-EXPANSION-015 — R semantic-namespace collision guard

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **R semantic-namespace collision guard**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Keep domain-29 generational R distinct from domain-30 source-local resistance R_resistance; never alias, substitute, compare, or merge them.

Scientific status: `preserved_unresolved`. Execution status: `structural_only`. Scientific provider domains: `[29]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
