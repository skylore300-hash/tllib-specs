# TLC-FC-30-EXPANSION-017 — Cultural-graft compatibility evaluator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Cultural-graft compatibility evaluator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Evaluate chi only with finite nonempty N_inv and supplied sim values; preserve the source average-of-maxima form.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Scientific provider domains: `[4]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
