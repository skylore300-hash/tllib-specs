# TLC-FC-30-EXPANSION-019 — Initial-penetration and local-speed descriptor

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Initial-penetration and local-speed descriptor**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Preserve penetration as only proportional to chi and v_loc approximately v_0 chi; do not invent the proportionality constant.

Scientific status: `partially_defined`. Execution status: `structural_only`. Scientific provider domains: `[]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
