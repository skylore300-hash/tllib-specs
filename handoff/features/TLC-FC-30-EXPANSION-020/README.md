# TLC-FC-30-EXPANSION-020 — Local-reinterpretation expression evaluator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Local-reinterpretation expression evaluator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Evaluate Msg prime=Msg+epsilon(C,H_loc) only with a supplied epsilon provider.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Scientific provider domains: `[]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
