# TLC-FC-30-EXPANSION-024 — School invariant-distance evaluator

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **School invariant-distance evaluator**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Evaluate d_E for externally assigned graph communities using supplied Xbar_E and source-local X_N representation.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Scientific provider domains: `[4, 29]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
