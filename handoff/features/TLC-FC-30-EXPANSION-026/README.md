# TLC-FC-30-EXPANSION-026 — Stable-diversification predicate

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Stable-diversification predicate**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Validate pairwise d_int <= D_max and mean d_E <= Delta_avg only with supplied providers, nonzero K, and supplied thresholds.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Scientific provider domains: `[4, 29]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
