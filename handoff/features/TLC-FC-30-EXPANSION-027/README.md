# TLC-FC-30-EXPANSION-027 — Expansion-threshold calibration provider boundary

This finalized Feature Handoff Package exposes the domain-30 Expansion feature **Expansion-threshold calibration provider boundary**. Its normative boundary is the authoritative source `maths/30-expansion/expansion.md` and the scientific providers declared for the feature.

Keep kappa_min, delta_adapt, delta_interp, theta_cent, D_max, and Delta_avg external and uncalibrated.

Scientific status: `external_provider_required`. Execution status: `structural_only`. Scientific provider domains: `[]`. Runtime domain dependencies are empty. The package deliberately leaves unresolved every quantity, calibration, provider, numerical method, normalization, and theorem that the source does not define.
