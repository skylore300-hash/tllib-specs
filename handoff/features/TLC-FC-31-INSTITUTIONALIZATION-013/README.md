# TLC-FC-31-INSTITUTIONALIZATION-013 — Heresy deviation predicate evaluator

This finalized Feature Handoff Package exposes the domain-31 Institutionalization feature **Heresy deviation predicate evaluator**. Its normative boundary is `maths/31-institutionalization/institutionalization.md` and the explicitly declared ownership/provider relations.

Evaluate only the strict deviation predicate delta_dog > epsilon_her from supplied compatible values; this is not by itself full heresy.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Confirmed scientific provider domains: `[4]`. Unresolved scientific relation domains: `[]`. Runtime domain dependencies are empty.

Forbidden: Do not use >=, calibrate epsilon_her or treat threshold exceedance alone as persistence after correction.
