# TLC-FC-31-INSTITUTIONALIZATION-020 — Role-value alignment predicate evaluator

This finalized Feature Handoff Package exposes the domain-31 Institutionalization feature **Role-value alignment predicate evaluator**. Its normative boundary is `maths/31-institutionalization/institutionalization.md` and the explicitly declared ownership/provider relations.

Evaluate the source inner-product predicate with supplied compatible values and theta_val, preserving >=.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Confirmed scientific provider domains: `[29]`. Unresolved scientific relation domains: `[]`. Runtime domain dependencies are empty.

Forbidden: Do not normalize vectors, choose cosine similarity, invent dimensions or calibrate theta_val.
