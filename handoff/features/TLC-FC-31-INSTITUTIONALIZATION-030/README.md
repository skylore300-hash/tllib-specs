# TLC-FC-31-INSTITUTIONALIZATION-030 — Reform invariant-fidelity validator

This finalized Feature Handoff Package exposes the domain-31 Institutionalization feature **Reform invariant-fidelity validator**. Its normative boundary is `maths/31-institutionalization/institutionalization.md` and the explicitly declared ownership/provider relations.

Validate the supplied reform output against ||pi_N(X_prime)-pi_N(X_orig)|| <= epsilon_ref using domain-04 projection authority.

Scientific status: `external_provider_required`. Execution status: `conditionally_executable`. Confirmed scientific provider domains: `[4]`. Unresolved scientific relation domains: `[]`. Runtime domain dependencies are empty.

Forbidden: Do not redefine pi_N, choose a norm, calibrate epsilon_ref, change <= or construct X_prime.
