# TLC-FC-32-DRIFT-AND-CORRECTION-004 — Suppression-test unavailable guard

The source names `kappa_supp` as the effect of simulated suppression but does not define the simulation or an executable test. This finalized structural guard therefore preserves the component as unavailable/provider-bound instead of inventing an intervention, causal estimator, graph deletion experiment, or simulator.

The package exposes only the unresolved scientific boundary. It does not fabricate executable semantics and introduces no `32 -> 35` dependency.