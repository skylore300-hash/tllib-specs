# TLC-FC-32-DRIFT-AND-CORRECTION-018 — Invariants non-replacement and companion-boundary guard

This finalized structural guard preserves the chapter statement that the Drift diagnostic score does not replace the fundamental invariants of domain 04. The confirmed scientific dependency is domain-level `32 -> 04`, with no fabricated feature-level executable mapping.

Fidelity 35 remains an analysis companion only. This package explicitly forbids promoting thematic proximity into `32 -> 35` or `35 -> 32`, and it introduces no runtime domain dependency.