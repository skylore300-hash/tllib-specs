# TLC-FC-35-FIDELITY-TO-INVARIANT-CORE-003

Implementation handoff for the temporal-core ambiguity guard. The source does not specify the temporal status of `x`; reject silent substitutions such as `x(t)`, `x(tau)`, or `x_v(t)`.
