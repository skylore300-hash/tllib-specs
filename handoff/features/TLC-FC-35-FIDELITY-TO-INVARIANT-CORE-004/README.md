# TLC-FC-35-FIDELITY-TO-INVARIANT-CORE-004

Implementation handoff for the exact generational variation expression. Evaluate only from supplied `epsilon_n`, `C_n`, and `pi_N(X_n)`; preserve the residual sign `X_n - pi_N(X_n)` and never sample stochastic variation internally.
