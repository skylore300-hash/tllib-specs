# TLC-FC-35-FIDELITY-TO-INVARIANT-CORE-005

Implementation handoff for the stochastic variation distribution descriptor `epsilon_n ~ Normal(0, Sigma)`. This package describes the source distribution only; it never creates an RNG, sampler, or covariance estimator.
