# TLC-FC-35-FIDELITY-TO-INVARIANT-CORE-006

Implementation handoff for `R_art(X) = min_{a in A_validated} ||X-pi_N(a)||`. Use only supplied validated candidates, projections and norm semantics; an empty validated-artifact set is a structured error, never a numeric fallback.
