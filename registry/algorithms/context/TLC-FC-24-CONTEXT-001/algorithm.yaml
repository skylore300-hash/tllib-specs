schema_version: 1
feature_id: TLC-FC-24-CONTEXT-001
algorithm_kind: structural_source_descriptor
execution: structural_only
steps:
  - require the five source component references in source order
  - preserve the inclusion C subset R^m times M
  - preserve M and Culture-referenced internals as opaque
  - emit a descriptor carrying TLC-UR-CONTEXT-001
forbidden: [constructing charts, tangent spaces, coordinates for M, Culture runtime calls]
