schema_version: 1
feature_id: TLC-FC-32-DRIFT-AND-CORRECTION-017
algorithm_kind: scientific_exclusion_guard
execution: structural_only
steps: [record zero denominator invalidity, expose exclusion descriptor, reject reconstruction repair and execution]
forbidden: [reconstruct_formula, replace_denominator, repair_formula, execute_metric]
