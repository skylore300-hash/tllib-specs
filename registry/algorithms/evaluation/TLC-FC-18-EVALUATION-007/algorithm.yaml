schema_version: 1
feature_id: TLC-FC-18-EVALUATION-007
algorithm_kind: conditional_threshold_expression_and_classifier
execution: conditionally_executable
steps: [require three expectation outputs and sigma provider, compute theta_1, compute theta_2, compute theta_3, verify interval-compatible threshold ordering without reordering, compare supplied metric norm, return thresholds and source zone]
forbidden: [invent_threshold, sort_thresholds, calibrate_thresholds, create_promotion_engine]
