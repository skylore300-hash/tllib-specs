schema_version: 1
feature_id: TLC-FC-18-EVALUATION-013
algorithm_kind: constrained_aggregation_expression
execution: conditionally_executable
steps: [require mentor peers self and external community evaluations, verify supplied source weights sum to one without modifying them, verify each alpha_i minimum, verify mentor minimum before supplied autonomy time, validate product exponent domain, form weighted sum, form beta times geometric product, add delta E_synergy, return E_total]
forbidden: [invent_community_evaluation_formula, silently_renormalize_source_weights, project_to_simplex, invent_autonomy_policy, invent_weight_minimum]
