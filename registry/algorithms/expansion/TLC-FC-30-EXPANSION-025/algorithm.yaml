schema_version: 1
feature_id: TLC-FC-30-EXPANSION-025
algorithm_kind: structural_descriptor
execution: structural_only
steps:
- Require a supplied school/community assignment before school-level metrics are evaluated.
forbidden:
- Do not invent a clustering, modularity, connectivity, or governance algorithm.
