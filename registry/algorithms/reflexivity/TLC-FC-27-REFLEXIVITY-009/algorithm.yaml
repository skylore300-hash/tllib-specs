schema_version: 1
feature_id: TLC-FC-27-REFLEXIVITY-009
algorithm_kind: structural_only_non_execution_guard
execution: structural_only
steps:
  - classify the request as literal source-expression access semantic correction or crisis lifecycle
  - preserve the positive operator signs and negative adaptive-gradient sign simultaneously
  - require explicit external semantic mode or provider before semantic correction
  - require an external crisis-lifecycle provider before lifecycle operations
  - otherwise return the corresponding structured non-execution error
forbidden: [silent sign selection, semantic formula repair, crisis state-machine invention, crisis duration or termination invention]
