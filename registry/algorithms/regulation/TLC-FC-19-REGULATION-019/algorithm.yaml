schema_version: 1
feature_id: TLC-FC-19-REGULATION-019
algorithm_kind: structural_descriptor
execution: structural_only
steps: [emit source operational constraints, emit source risks, emit source safeguards, attach optional supplied evidence, return descriptor]
forbidden: [algorithmize_governance, infer_compliance, choose_thresholds]
