schema_version: 1
domain: transmission-lifecycle
status: finalized
module: transmission-lifecycle
scientific_domain_dependencies:
- 21
- 29
unresolved_scientific_domain_dependencies:
- 4
runtime_domain_dependencies: []
constraints:
- preserve the four operational macro-stages and seven specialized phases without inventing a bijection
- preserve the finite automaton as partial and retain only source-backed transitions
- namespace homonymous symbols by document, section and scientific role
- preserve every source-local X state and all absent cross-phase mappings
- preserve undefined zero-denominator, empty-set, zero-vector and negative-time cases
- preserve opposite coherence-gradient signs and the integration threshold-2/threshold-3 divergence
- do not prescribe argmin solvers, numerical integrators, stochastic laws, timesteps, seeds or convergence rules
- preserve bifurcation and irreversibility statements as claims rather than algorithms or completed proofs
- reuse Domain 21 only for explicitly referenced fairness corrections
- reuse Domain 29 only for the explicit generational-propagation/G_t handoff
- keep Domain 04 projection authority unresolved and do not import Domain 17 from common wave membership
- runtime dependencies remain empty
- reuse exactly eight shared contracts
