schema_version: 1
feature_id: TLC-FC-30-EXPANSION-022
oracle_kind: source_conformance
acceptance:
- Evaluate H_cent from supplied V_t and c values only when the common denominator is defined.
rejections:
- Do not invent c(v), silently normalize alternate data, or return a zero-denominator fallback.
