schema_version: 1
feature_id: TLC-FC-29-GENERATIONAL-PROPAGATION-001
oracle_kind: exact_predicate
acceptance: [comparators are >= >= <= >= > in source order, all five conditions are conjunctive, thresholds are supplied]
rejections: [invented thresholds, aggregated score, relaxed strict F_trans comparator]
